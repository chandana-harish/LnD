apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-deployment 
  namespace: ld
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ld
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: ld
    spec:
      containers:
        - name: api-cont
          image: chandu554/api-gateway-lb-service:latest
          ports:
            - containerPort: 3003
          env:
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: api-secret
                  key: MONGO_URI
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: api-config
                  key: PORT
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "200m"
              memory: "512Mi"